const numA = parseFloat(prompt('Informe o  primeiro numero:'));
const numB = parseFloat(prompt('Informe o  segundo numero:'));

const soma = (numA + numB)
const subtracao = (numA - numB)
const divisao = (numA / numB)
const multiplicacao = (numA * numB)


alert("A soma é: " + soma + 
      "\nA subtração é: " + subtracao +
      "\nA divisão é: " + divisao + 
      "\nA multiplicação é: " + multiplicacao
);


